# SET up repository

##clone repo
`git clone `

##cd
`cd startup`

##set up env
`python3 -m venv env`

## activate env
`source env/bin/activate`

## install requirements
`pip install -r requierements.txt`

## migrate
`python manage.py migrate`